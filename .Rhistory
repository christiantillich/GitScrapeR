sha          = x$sha
,filename     = x$filename
#     ,status       = x$status
#     ,additions    = x$additions
#     ,deletions    = x$deletions
#     ,changes      = x$changes
# ,patch        = x$patch
)
}
lapply(thing$files, files.parse) %>% head
files.parse <- function(x) {
print(x$filename)
data.frame(
sha          = x$sha
,filename     = x$filename
#     ,status       = x$status
#     ,additions    = x$additions
#     ,deletions    = x$deletions
#     ,changes      = x$changes
# ,patch        = x$patch
) %>% print
}
lapply(thing$files, files.parse) %>% head
files.parse <- function(x) {
print(i++)
data.frame(
sha          = x$sha
,filename     = x$filename
#     ,status       = x$status
#     ,additions    = x$additions
#     ,deletions    = x$deletions
#     ,changes      = x$changes
# ,patch        = x$patch
)
}
lapply(thing$files, files.parse) %>% head
files.parse <- function(x) {
print(i++)
data.frame(
sha          = x$sha
,filename     = x$filename
#     ,status       = x$status
#     ,additions    = x$additions
#     ,deletions    = x$deletions
#     ,changes      = x$changes
# ,patch        = x$patch
)
}
lapply(thing$files, files.parse) %>% head
i=0
files.parse <- function(x) {
i <- i+1; print(i)
data.frame(
sha          = x$sha
,filename     = x$filename
#     ,status       = x$status
#     ,additions    = x$additions
#     ,deletions    = x$deletions
#     ,changes      = x$changes
# ,patch        = x$patch
)
}
lapply(thing$files, files.parse) %>% head
i=0
files.parse <- function(x) {
i <- i+1
print(i)
data.frame(
sha          = x$sha
,filename     = x$filename
#     ,status       = x$status
#     ,additions    = x$additions
#     ,deletions    = x$deletions
#     ,changes      = x$changes
# ,patch        = x$patch
)
}
lapply(thing$files, files.parse) %>% head
i = 0
files.parse <- function(x) {
# i <- i+1
print(i)
data.frame(
sha          = x$sha
,filename     = x$filename
#     ,status       = x$status
#     ,additions    = x$additions
#     ,deletions    = x$deletions
#     ,changes      = x$changes
# ,patch        = x$patch
)
}
lapply(thing$files, files.parse) %>% head
files.parse <- function(x) {
i <- i+1
print(i)
data.frame(
sha          = x$sha
,filename     = x$filename
#     ,status       = x$status
#     ,additions    = x$additions
#     ,deletions    = x$deletions
#     ,changes      = x$changes
# ,patch        = x$patch
)
}
lapply(thing$files, files.parse) %>% head
i = 0
thing$files %>% head
files.parse <- function(x) {
print(x$sha)
data.frame(
sha          = x$sha
,filename     = x$filename
#     ,status       = x$status
#     ,additions    = x$additions
#     ,deletions    = x$deletions
#     ,changes      = x$changes
# ,patch        = x$patch
)
}
lapply(thing$files, files.parse) %>% head
files.parse <- function(x) {
data.frame(
sha          = x$sha
# ,filename     = x$filename
#     ,status       = x$status
#     ,additions    = x$additions
#     ,deletions    = x$deletions
#     ,changes      = x$changes
# ,patch        = x$patch
)
}
test <- lapply(thing$files, files.parse)
test
test[[301]]
thing[[301]]
thing[[300]]
thing[300]
thing[299]
thing$files[300]
thing$files[301]
thing$files[301]$sha
files.parse <- function(x) {
data.frame(
sha          = x$sha %||% ''
# ,filename     = x$filename
#     ,status       = x$status
#     ,additions    = x$additions
#     ,deletions    = x$deletions
#     ,changes      = x$changes
# ,patch        = x$patch
)
}
test <- lapply(thing$files, files.parse)
test
files.parse <- function(x) {
data.frame(
sha          = x$sha %||% ''
,filename     = x$filename
#     ,status       = x$status
#     ,additions    = x$additions
#     ,deletions    = x$deletions
#     ,changes      = x$changes
# ,patch        = x$patch
)
}
test <- lapply(thing$files, files.parse)
test
files.parse <- function(x) {
data.frame(
sha          = x$sha %||% ''
,filename     = x$filename
,status       = x$status
,additions    = x$additions
,deletions    = x$deletions
,changes      = x$changes
,patch        = x$patch
)
}
test <- lapply(thing$files, files.parse)
files.parse <- function(x) {
data.frame(
sha          = x$sha %||% ''
,filename     = x$filename
,status       = x$status
#     ,additions    = x$additions
#     ,deletions    = x$deletions
#     ,changes      = x$changes
# ,patch        = x$patch
)
}
test <- lapply(thing$files, files.parse)
broken_key = "40c7cba74988fb7756ca8f0fba4adf79ad8774fb"
test %>% head
files.parse <- function(x) {
data.frame(
sha          = x$sha %||% ''
,filename     = x$filename
,status       = x$status
,additions    = x$additions
#     ,deletions    = x$deletions
#     ,changes      = x$changes
# ,patch        = x$patch
)
}
test <- lapply(thing$files, files.parse)
test
files.parse <- function(x) {
data.frame(
sha          = x$sha %||% ''
,filename     = x$filename
,status       = x$status
,additions    = x$additions
,deletions    = x$deletions
#     ,changes      = x$changes
# ,patch        = x$patch
)
}
test <- lapply(thing$files, files.parse)
files.parse <- function(x) {
data.frame(
sha          = x$sha %||% ''
,filename     = x$filename
,status       = x$status
,additions    = x$additions
,deletions    = x$deletions
,changes      = x$changes
# ,patch        = x$patch
)
}
test <- lapply(thing$files, files.parse)
files.parse <- function(x) {
data.frame(
sha          = x$sha %||% ''
,filename     = x$filename
,status       = x$status
,additions    = x$additions
,deletions    = x$deletions
,changes      = x$changes
,patch        = x$patch
)
}
test <- lapply(thing$files, files.parse)
thing <- show.github.path(url)
files.parse <- function(x) {
data.frame(
sha          = x$sha %||% ''
,filename     = x$filename
,status       = x$status
,additions    = x$additions
,deletions    = x$deletions
,changes      = x$changes
,patch        = x$patch %||% ''
)
}
test <- lapply(thing$files, files.parse)
head(test)
test[[300]]
?gsub
files.parse <- function(x) {
data.frame(
sha          = x$sha %||% ''
,path         = gsub('[^/]+$','',x$filename)
,filename     = x$filename
,status       = x$status
,additions    = x$additions
,deletions    = x$deletions
,changes      = x$changes
,patch        = x$patch %||% ''
)
}
test <- lapply(thing$files, files.parse)
head(test)
files.parse <- function(x) {
data.frame(
sha          = x$sha %||% ''
,path         = gsub('[^/]+$','',x$filename)
,filename     = gsub('.*/','',x$filename)
,status       = x$status
,additions    = x$additions
,deletions    = x$deletions
,changes      = x$changes
,patch        = x$patch %||% ''
)
}
test <- lapply(thing$files, files.parse)
head(test)
test$patch %>% head
head(test)
test$patch
test %>% class
test <- lapply(thing$files, files.parse) %>%
do.call(rbind, .) %>%
as.data.frame
head(test)
test$patch %>% head
files.parse <- function(x) {
data.frame(
sha          = x$sha %||% NA
,path         = gsub('[^/]+$','',x$filename)
,filename     = gsub('.*/','',x$filename)
,status       = x$status
,additions    = x$additions
,deletions    = x$deletions
,changes      = x$changes
,patch        = x$patch %||% NA
)
}
test <- lapply(thing$files, files.parse) %>%
do.call(rbind, .) %>%
as.data.frame
test$patch %>% head
test$patch %>% is.na %>% head
test$patch %>% is.na %>% test$patch[.]
test$patch %>% is.na %>% test$patch[!.]
?`!`
?not
not(T)
test$patch %>% is.na %>% not %>% test$patch[!.]
test$patch %>% is.na %>% not %>% test$patch[.]
files.parse <- function(x) {
data.frame(
sha          = x$sha %||% NA
,path         = gsub('[^/]+$','',x$filename)
,filename     = gsub('.*/','',x$filename)
,status       = x$status
,additions    = x$additions
,deletions    = x$deletions
,changes      = x$changes
,patch        = as.character(x$patch %||% NA)
)
}
test <- lapply(thing$files, files.parse) %>%
do.call(rbind, .) %>%
as.data.frame
test$patch %>% is.na %>% not %>% test$patch[.]
test$patch <- as.character(test$patch)
test$patch %>% is.na %>% not %>% test$patch[.]
changelog <- test$patch %>% is.na %>% not %>% test$patch[.]
changelog[1]
changelog[2]
changelog[3]
changelog[5]
changelog[1]
changelog[2]
changelog[3]
changelog[4]
files.parse <- function(x) {
print(x$patch)
data.frame(
sha          = x$sha %||% NA
,path         = gsub('[^/]+$','',x$filename)
,filename     = gsub('.*/','',x$filename)
,status       = x$status
,additions    = x$additions
,deletions    = x$deletions
,changes      = x$changes
,patch        = as.character(x$patch %||% NA)
)
}
test <- lapply(head(thing$files), files.parse) %>%
do.call(rbind, .) %>%
as.data.frame
thing$files[[2]]
thing$files[[1]]
thing$files[[0]]
thing$files[[1]]
thing$files[[2]]
files.parse <- function(x) {
print(x$patch)
data.frame(
sha          = x$sha %||% NA
,path         = gsub('[^/]+$','',x$filename)
,filename     = gsub('.*/','',x$filename)
,status       = x$status
,additions    = x$additions
,deletions    = x$deletions
,changes      = x$changes
,patch        = as.character(x$patch %||% NA)
)
}
test <- lapply(thing$files[2], files.parse) %>%
do.call(rbind, .) %>%
as.data.frame
test
files.parse <- function(x) {
print(x$patch)
data.frame(
sha          = x$sha %||% NA
,path         = gsub('[^/]+$','',x$filename)
,filename     = gsub('.*/','',x$filename)
,status       = x$status
,additions    = x$additions
,deletions    = x$deletions
,changes      = x$changes
,patch        = as.character(x$patch %||% NA)
)
}
test <- lapply(thing$files[3], files.parse) %>%
do.call(rbind, .) %>%
as.data.frame
test[3]
test
thing[3]
thing$files[3]
test
thing$files[3]
files.parse <- function(x) {
print(x$patch)
data.frame(
sha               = x$sha %||% NA
,path              = gsub('[^/]+$','',x$filename)
,filename          = gsub('.*/','',x$filename)
,previous_filename = x$previous_filename
,status            = x$status
,additions         = x$additions
,deletions         = x$deletions
,changes           = x$changes
,patch             = as.character(x$patch %||% NA)
)
}
files.parse <- function(x) {
print(x$patch)
data.frame(
sha               = x$sha %||% NA
,path              = gsub('[^/]+$','',x$filename)
,filename          = gsub('.*/','',x$filename)
,previous_filename = x$previous_filename %||% NA
,status            = x$status
,additions         = x$additions
,deletions         = x$deletions
,changes           = x$changes
,patch             = as.character(x$patch %||% NA)
)
}
test <- lapply(thing$files[3], files.parse) %>%
do.call(rbind, .) %>%
as.data.frame
test$patch <- as.character(test$patch)
test
test <- lapply(thing$files[4], files.parse) %>%
do.call(rbind, .) %>%
as.data.frame
test$patch <- as.character(test$patch)
test
thing$file[4]
files.parse <- function(x) {
print(x$patch)
data.frame(
sha               = x$sha %||% NA
,path              = gsub('[^/]+$','',x$filename)
,filename          = gsub('.*/','',x$filename)
,previous_filename = x$previous_filename %||% NA
,status            = x$status
,additions         = x$additions
,deletions         = x$deletions
,changes           = x$changes
,patch             = as.character(x$patch %||% NA)
)
}
test <- lapply(thing$files, files.parse) %>%
do.call(rbind, .) %>%
as.data.frame
test$patch <- as.character(test$patch)
files.parse <- function(x) {
data.frame(
sha               = x$sha %||% NA
,path              = gsub('[^/]+$','',x$filename)
,filename          = gsub('.*/','',x$filename)
,previous_filename = x$previous_filename %||% NA
,status            = x$status
,additions         = x$additions
,deletions         = x$deletions
,changes           = x$changes
,patch             = as.character(x$patch %||% NA)
)
}
test <- lapply(thing$files, files.parse) %>%
do.call(rbind, .) %>%
as.data.frame
test$patch <- as.character(test$patch)
test$patch %>% is.na %>% test[.,]
thing$files[4]
test$status
test$status %>% unique
test[test$status == 'removed',]
test[test$status == 'removed',] %>% head
thing$files[286]
test$status %>% unique
files.parse <- function(x) {
data.frame(
sha               = x$sha %||% NA
,path              = gsub('[^/]+$','',x$filename)
,filename          = gsub('.*/','',x$filename)
,previous_filename = x$previous_filename %||% NA
,status            = x$status
,additions         = x$additions
,deletions         = x$deletions
,changes           = x$changes
,patch             =
if(x$status == 'added') as.character(GET(x$raw_url)) else(
as.character(x$patch %||% NA)
)
)
}
test <- lapply(thing$files, files.parse) %>%
do.call(rbind, .) %>%
as.data.frame
head(test)
test$patch <- as.character(test$patch)
colnames(test)
